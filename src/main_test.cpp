#include <iostream>
#include <XBotInterface/RobotInterface.h>
#include <XBotInterface/sync_flags.h>

////////////////////////////////////////////////////
// Main
////////////////////////////////////////////////////


int main(int argc, char *argv[])
{
    
    using namespace XBot;
    
    bool p = true, v = true, a = true, e = true;
    
//     parseFlags(p,v,a,e, Sync::Position, Sync::Effort, Sync::Acceleration);
//     
//     if(p) std::cout << "SYNC POS!!" << std::endl;
//     if(v) std::cout << "SYNC VEL!!" << std::endl;
//     if(a) std::cout << "SYNC ACC!!" << std::endl;
//     if(e) std::cout << "SYNC EFF!!" << std::endl;
    
}

